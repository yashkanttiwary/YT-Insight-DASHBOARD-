import { get, set, del } from "idb-keyval";export const storage = {  async get(key: string): Promise<string | null> {    try {      const val = await get(key);      return val !== undefined ? val : null;    } catch (e) {      console.error("Storage get error", e);      return null;    }  },  async set(key: string, value: string): Promise<void> {    try {      await set(key, value);    } catch (e) {      console.error("Storage set error", e);    }  },  async remove(key: string): Promise<void> {    try {      await del(key);    } catch (e) {      console.error("Storage remove error", e);    }  }};
